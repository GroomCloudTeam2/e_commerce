spring:
  application:
    name: e-commerce

  datasource:
    # 👇 중요: 아까 도커 설정(POSTGRES_DB)과 이름을 똑같이 맞춰야 연결됩니다!
    url: jdbc:postgresql://localhost:5432/ecommerce
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
        # 👇 (선택) PostgreSQL 전용 방언 설정 (없어도 되지만 넣으면 더 안정적)
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # h2:  <-- PostgreSQL 쓸 때는 필요 없으니 지웠습니다.
  #   console:
  #     enabled: true

  security:
    user:
      name: user
      password: 1234


jwt:
  secret: your-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm
  access-token-validity: 3600000
  refresh-token-validity: 604800000

logging:
  level:
    com.groom.ecommerce: DEBUG
    org.hibernate.SQL: DEBUG

toss:
  payments:
    base-url: https://api.tosspayments.com
    secret-key: ${TOSS_PAYMENTS_SECRET_KEY:}
    client-key: test_ck_AQ92ymxN349xlKM196KKVajRKXvd
    success-url: http://localhost:8080/pay-success.html
    fail-url: http://localhost:8080/pay-fail.html
    connect-timeout-ms: 3000
    read-timeout-ms: 5000

